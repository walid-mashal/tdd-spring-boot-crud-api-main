This project provides REST API End points for inserting, updating, deleting and fetching user record(s)

Insert: Send a POST request to localhost:8080/api/user
Update: Send a PUT request to localhost:8080/api/user/{id}
Delete: Send a DELETE request to localhost:8080/api/user/{id}
Fetch: Send a GET request to localhost:8080/api/user/{id}